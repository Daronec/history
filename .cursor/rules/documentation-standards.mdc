---
globs: *.md,*.rst,*.txt
description: Стандарты документации проекта
---

# Стандарты документации

## Структура README.md

### Основные разделы
```markdown
# ИИ-История

## 📋 Описание
Краткое описание проекта и его целей.

## 🚀 Быстрый старт
Пошаговые инструкции для начала работы.

## 📁 Структура проекта
Описание основных директорий и файлов.

## 🛠️ Установка
Инструкции по установке зависимостей.

## 🎓 Обучение моделей
Руководство по обучению ИИ моделей.

## 🖥️ Интерфейсы
Описание доступных интерфейсов.

## 📊 Версии exe файлов
Сравнение различных версий исполняемых файлов.

## 🤝 Вклад в проект
Инструкции для разработчиков.

## 📄 Лицензия
Информация о лицензии.
```

## Документация кода

### Docstrings для функций
```python
def train_model(data_path, model_name, epochs=3):
    """
    Обучает модель на предоставленных данных.
    
    Args:
        data_path (str): Путь к файлу с данными для обучения
        model_name (str): Название базовой модели (например, 'distilgpt2')
        epochs (int, optional): Количество эпох обучения. По умолчанию 3.
    
    Returns:
        str: Путь к сохраненной обученной модели
    
    Raises:
        FileNotFoundError: Если файл данных не найден
        ValueError: Если данные имеют неподдерживаемый формат
    
    Example:
        >>> model_path = train_model('data/history.txt', 'distilgpt2', epochs=5)
        >>> print(f"Модель сохранена в: {model_path}")
    """
    pass
```

### Комментарии в коде
```python
# Загружаем токенизатор для выбранной модели
tokenizer = AutoTokenizer.from_pretrained(model_name)

# Добавляем специальные токены для лучшего качества генерации
special_tokens = {
    "pad_token": "<pad>",
    "eos_token": "<|endoftext|>",
    "bos_token": "<|startoftext|>"
}
tokenizer.add_special_tokens(special_tokens)

# Настраиваем параметры обучения
training_args = TrainingArguments(
    output_dir='./models',           # Папка для сохранения модели
    num_train_epochs=epochs,        # Количество эпох
    per_device_train_batch_size=4,  # Размер батча
    # ... остальные параметры
)
```

## Руководства пользователя

### Структура руководства
```markdown
# Руководство по [название]

## 🎯 Цель
Для чего предназначено это руководство.

## 📋 Предварительные требования
Что нужно установить/настроить перед началом.

## 🚀 Пошаговые инструкции
Детальные шаги с примерами.

## ⚠️ Возможные проблемы
Частые ошибки и их решения.

## 💡 Советы и рекомендации
Дополнительная информация для эффективного использования.

## 📞 Поддержка
Куда обращаться за помощью.
```

## Документация API

### Описание функций
```markdown
## generate_text(prompt, model_name, max_length=200)

Генерирует текст на основе промпта с использованием указанной модели.

### Параметры
- `prompt` (str): Текст-запрос для генерации
- `model_name` (str): Название модели ('english' или 'russian')
- `max_length` (int): Максимальная длина генерируемого текста

### Возвращаемое значение
- `str`: Сгенерированный текст

### Примеры использования
```python
# Генерация на английском
result = generate_text("The history of", "english", max_length=150)

# Генерация на русском
result = generate_text("История России", "russian", max_length=200)
```
```

## Changelog

### Формат записей
```markdown
# Changelog

## [1.2.0] - 2024-01-15

### Добавлено
- Поддержка русской модели ИИ
- Веб-интерфейс для работы с моделями
- Возможность загрузки PDF файлов

### Изменено
- Улучшена обработка ошибок в GUI
- Оптимизирован процесс обучения моделей

### Исправлено
- Исправлена проблема с кодировкой на Windows
- Устранена ошибка загрузки больших файлов

## [1.1.0] - 2024-01-01

### Добавлено
- Первая версия Windows интерфейса
- Поддержка обучения на CSV данных
```